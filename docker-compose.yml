services:
  # 1. PostgreSQL 데이터베이스
  db:
    image: postgres:16
    restart: always
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    networks:
      - my_network # 👈 네트워크 설정 추가

  # 2. NestJS 백엔드 API
  api:
    build:
      context: . # 루트에서 빌드 시작
      dockerfile: apps/api/Dockerfile # api Dockerfile 사용
    ports:
      - '3001:3001'
    environment:
      # ⭐️ 중요: 'localhost'가 아니라 서비스 이름인 'db'로 접속
      DATABASE_URL: 'postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}'
      JWT_SECRET: ${JWT_SECRET}
      PORT: 3001 # main.ts에서 사용하는 포트
    depends_on:
      - db # db가 실행된 후에 api 실행
    networks:
      - my_network # 👈 네트워크 설정 추가

  # 3. Next.js 프론트엔드 WEB
  web:
    build:
      context: . # 루트에서 빌드 시작
      dockerfile: apps/web/Dockerfile # web Dockerfile 사용
    ports:
      - '3000:3000'
    environment:
      # ⭐️ 중요: 브라우저가 아닌, 컨테이너 내부에서 API를 호출할 주소
      # 서비스 이름인 'api'와 api의 내부 포트 '3001'
      NEXT_PUBLIC_API_URL: 'http://api:3001'
      PORT: 3000
    depends_on:
      - api # api가 실행된 후에 web 실행
    networks:
      - my_network # 👈 네트워크 설정 추가

# Docker 컨테이너들이 서로 통신할 수 있는 가상 네트워크
networks:
  my_network:
    driver: bridge
