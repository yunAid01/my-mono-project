# ----------------------------------------------------------------
# 1. 의존성 (Dependencies)
# ----------------------------------------------------------------
# node_modules는 절대 올리면 안 됩니다. (루트 및 모든 하위 폴더)
**/node_modules

# pnpm 디버그 로그
pnpm-debug.log


# ----------------------------------------------------------------
# 2. 비밀 파일 (Secrets) - 매우 중요!
# ----------------------------------------------------------------
# .env 파일은 DB 비밀번호 등이 있으므로 절대 올리면 안 됩니다.
# (루트 및 모든 하위 폴더의 .env)
**/.env
**/.env.*
# .env 파일 예시 파일은 올려도 됩니다.
!.env.example


# ----------------------------------------------------------------
# 3. 프레임워크 빌드 결과물 (Frameworks)
# ----------------------------------------------------------------
# Next.js (apps/web)
**/.next/
**/out/

# NestJS (apps/api) // packages/**
**/dist/


# ----------------------------------------------------------------
# 4. 도구 및 OS (Tools & OS)
# ----------------------------------------------------------------
# VS Code 설정
.vscode/

# MacOS
.DS_Store

# Windows
Thumbs.db

# 로그 파일
*.log
npm-debug.log*
yarn-error.log*


# ----------------------------------------------------------------
# 5. 데이터베이스 (Database)
# ----------------------------------------------------------------
# Docker 볼륨으로 생성된 DB 데이터 (절대 올리면 안 됨)
postgres-data/

# (혹시 나중에 SQLite를 쓴다면)
*.db
*.db-journal